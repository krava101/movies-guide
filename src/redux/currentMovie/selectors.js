export const selectCurrentMovie = state => state.currentMovie.movie;

export const selectCast = state => state.currentMovie.cast;

export const selectReviews = state => state.currentMovie.reviews;

export const selectSimilar = state => state.currentMovie.similar;