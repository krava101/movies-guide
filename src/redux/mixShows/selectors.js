export const selectMixShows = state => state.mixShows.items;

export const selectMixShowsTotalPages = state => state.mixShows.totalPages ;

export const selectIsMixLoading = state => state.mixShows.isLoading;